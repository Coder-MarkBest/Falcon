<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".DemoApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light">

        <!-- Client UI runs in the main process -->
        <activity
            android:name=".DemoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============ Falcon framework components (run in the :server process) ============ -->

        <!-- Binder host: serves all IPC calls. Must run in the service-hosting process. -->
        <service
            android:name="com.falcon.ipc.core.IpcHostService"
            android:exported="false"
            android:process=":server">
            <intent-filter>
                <action android:name="com.falcon.ipc.HOST_SERVICE" />
            </intent-filter>
        </service>

        <!-- Service registry / discovery (ContentProvider Binder passthrough, approach B).
             The authority MUST equal "${applicationId}.falcon.registry".
             Lives in the same process as IpcHostService so it can hand out its Binder. -->
        <provider
            android:name="com.falcon.ipc.core.IpcRegistryProvider"
            android:authorities="${applicationId}.falcon.registry"
            android:exported="true"
            android:process=":server" />

        <!-- For cross-app: declare target packages in <queries>.
             <queries>
                 <package android:name="com.oem.nav" />
             </queries>
        -->

    </application>
</manifest>
