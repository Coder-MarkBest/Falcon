<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".BenchmarkApp"
        android:label="Falcon Benchmark"
        android:theme="@style/Theme.AppCompat.Light">
        <activity
            android:name=".BenchmarkActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AIDL host (existing) -->
        <service
            android:name=".BenchmarkHostService"
            android:exported="false"
            android:process=":benchmark_remote" />

        <!-- Messenger host — separate service to avoid Android deduplicating binds
             to the same component name (which would give MessengerTest an AIDL binder). -->
        <service
            android:name=".BenchmarkMessengerService"
            android:exported="false"
            android:process=":benchmark_remote" />

        <!-- Falcon host, in the remote process; bound directly by FalconTest -->
        <service
            android:name="com.falcon.ipc.core.IpcHostService"
            android:exported="false"
            android:process=":benchmark_remote">
            <intent-filter>
                <action android:name="com.falcon.ipc.HOST_SERVICE" />
            </intent-filter>
        </service>

        <!-- Falcon service registry — required by PeerManager for discovery.
             The authority must match FalconManager's hardcoded URI pattern:
             content://${applicationId}.falcon.registry/services -->
        <provider
            android:name="com.falcon.ipc.core.IpcRegistryProvider"
            android:authorities="${applicationId}.falcon.registry"
            android:exported="false"
            android:process=":benchmark_remote" />

        <!-- ContentProvider benchmark target, in the remote process -->
        <provider
            android:name=".BenchmarkProvider"
            android:authorities="${applicationId}.falcon.benchmark.provider"
            android:exported="false"
            android:process=":benchmark_remote" />

        <!-- Broadcast request receiver, in the remote process -->
        <receiver
            android:name=".BenchmarkRequestReceiver"
            android:exported="false"
            android:process=":benchmark_remote">
            <intent-filter>
                <action android:name="com.falcon.benchmark.BENCH_REQUEST" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
